# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=group_memos
DB_USER=postgres
DB_PASSWORD=your_password

# 또는 DATABASE_URL 사용 (클라우드 배포시)
# DATABASE_URL=postgresql://user:password@host:5432/database

# JWT 설정
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# 서버 포트
PORT=3000

